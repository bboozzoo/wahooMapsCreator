# wahooMapsCreator requirements (based on conda-env/gdal-user.yml)
# wahoomc itself is installed from source (pip install .), not from PyPI
beautifulsoup4>=4.11,<5
geojson>=2.5,<3
lxml>=6.0,<7  # lxml 6.x ships cp314 wheels (core26 = Python 3.14)
matplotlib>=3.10,<3.11  # >=3.10.5 ships cp314 wheels (core26 = Python 3.14)
packaging>=21.0

# GDAL python bindings: must be <= the installed system GDAL version
# (core26 / Ubuntu 26.04 ships GDAL 3.12.2; for the snap, use GDAL==3.12.2)
GDAL>=3.6,<3.13

# Optional: OSM tools for contour lines are system packages, not pip:
#   osmium-tool, osmosis